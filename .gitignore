# Binaries
/statushub
/statushubctl
/bin/
*.exe

# Test and coverage output
*.out
*.test
coverage.*

# Local environment
.env
.env.*
!.env.example

# Editors and OS
.DS_Store
.idea/
.vscode/

# Dashboard build inputs (the built dist is committed so a Go build needs no node)
web/node_modules/
sdk/node/node_modules/
sdk/node/dist/
sdk/python/.venv/
sdk/python/dist/
__pycache__/
sdk/node/package-lock.json

# Terraform working state; the module is source, the lock and plugins are not
deploy/terraform/.terraform/
deploy/terraform/.terraform.lock.hcl
deploy/terraform/*.tfstate*
